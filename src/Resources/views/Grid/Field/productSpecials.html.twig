<ul style="margin: 0; padding: 0;">
    {% for promotion in data %}
        <li style="list-style-type: none;">
            {{ loop.index }}. <a href="{{ path('setono_sylius_catalog_promotions_admin_promotion_update', { 'id': promotion.id }) }}">
                {{ promotion.name }}
            </a>
        </li>
    {% endfor %}
</ul>
