<ul style="margin: 0; padding: 0;">
    {% for special in data %}
        <li style="list-style-type: none;">
            {{ loop.index }}. <a href="{{ path('setono_sylius_bulk_specials_admin_special_update', { 'id': special.id }) }}">
                {{ special.name }}
            </a>
        </li>
    {% endfor %}
</ul>